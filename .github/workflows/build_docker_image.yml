name: Build docker image

on:
  push:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  foo:
    runs-on: ubuntu-20.04
    timeout-minutes: 30
    steps:
      - name: Build docker image
        run: |
          docker build -t test .
